remote_theme: jekyll/minima
plugins:
  - jekyll-remote-theme
minima:
  skin: dark

  # Default layout for pages
defaults:
  - scope:
      path: ""
      type: "pages"
    values:
      layout: "page"

